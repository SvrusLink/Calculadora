#include <iostream>
#include <cmath> 

// Declaración de la función
double elevarAPotencia(double base, double exponente);

double elevarAPotencia(double base, double exponente) {
    return pow(base, exponente); //pow de la biblioteca <cmath>
}
